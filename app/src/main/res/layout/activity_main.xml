<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:id="@+id/activity_main_lv_listview"
        android:layout_above="@+id/activity_main_txt_todoitem" />

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/activity_main_txt_todoitem"
        android:layout_toLeftOf="@+id/activity_main_btn_add"
        android:layout_toStartOf="@+id/activity_main_btn_add"
        android:layout_alignParentBottom="true"
        android:hint="Add a new item" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/activity_main_btn_add"
        android:id="@+id/activity_main_btn_add"
        android:layout_alignTop="@+id/activity_main_txt_todoitem"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"/>

</RelativeLayout>
